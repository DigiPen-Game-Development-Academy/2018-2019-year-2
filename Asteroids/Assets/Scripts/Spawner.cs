using System.Collections;
using System.Collections.Generic;
using UnityEngine;
    
public class Spawner : MonoBehaviour {
    GameObject Asteroid;
    const int Maxasteroids = 99;
    float Timer = 0.00f;
    int rocksalive = 0;
    // Use this for initialization
    void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {
        //Timer = Time.deltaTime;
        //if (Timer >= 2.0f) {
        //    Timer = 0.0f;
        //    rocksalive++;
        //    if() { }
        //}


        
	}
}
